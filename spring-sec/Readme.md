// In GreetingsController:
@Autowired
private AuthenticationManager authenticationManager;  // ← Needs this bean!

@PostMapping("/signin")
public ResponseEntity<?> authenticateUser(@RequestBody LoginRequest loginRequest) {
Authentication auth = authenticationManager.authenticate(...);  // ← Uses it here
}
```

**Without this bean:** You'd get:
```
No qualifying bean of type 'AuthenticationManager' available
```

---

## 🔄 Complete Authentication Flow with This Config

Let me map everything together:

### **Flow 1: Login Request**
```
POST /signin
Body: { "username": "user1", "password": "password1" }

┌─────────────────────────────────────────────────────────────┐
│ 1. Request enters SecurityFilterChain                        │
├─────────────────────────────────────────────────────────────┤
│   authorizeRequests:                                         │
│     /signin → permitAll() ✅                                 │
│   sessionManagement:                                         │
│     STATELESS (no session created)                           │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 2. AuthTokenFilter.doFilterInternal()                        │
├─────────────────────────────────────────────────────────────┤
│   parseJwt(request) → null (no Authorization header)         │
│   Skip JWT authentication                                    │
│   filterChain.doFilter() → Continue                          │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 3. GreetingsController.authenticateUser()                    │
├─────────────────────────────────────────────────────────────┤
│   authenticationManager.authenticate(                        │
│       new UsernamePasswordAuthenticationToken(               │
│           "user1", "password1"                               │
│       )                                                      │
│   )                                                          │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 4. AuthenticationManager delegates to                        │
│    DaoAuthenticationProvider                                 │
├─────────────────────────────────────────────────────────────┤
│   Step 1: userDetailsService.loadUserByUsername("user1")     │
│           ↓                                                  │
│           JdbcUserDetailsManager                             │
│           ↓                                                  │
│           SQL: SELECT * FROM users WHERE username='user1'    │
│           ↓                                                  │
│           Returns: UserDetails {                             │
│               username: "user1",                             │
│               password: "$2a$10$...",                        │
│               authorities: [ROLE_USER]                       │
│           }                                                  │
│                                                              │
│   Step 2: passwordEncoder.matches(                           │
│               "password1",  ← from request                   │
│               "$2a$10$..."  ← from database                  │
│           )                                                  │
│           ↓                                                  │
│           BCryptPasswordEncoder verifies                     │
│           ↓                                                  │
│           true ✅                                            │
│                                                              │
│   Step 3: Create authenticated token                         │
│           UsernamePasswordAuthenticationToken {              │
│               principal: UserDetails("user1"),               │
│               credentials: null,                             │
│               authorities: [ROLE_USER],                      │
│               authenticated: true                            │
│           }                                                  │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Back to Controller                                        │
├─────────────────────────────────────────────────────────────┤
│   SecurityContextHolder.setAuthentication(authentication)    │
│   Generate JWT token                                         │
│   Return JWT to client                                       │
└─────────────────────────────────────────────────────────────┘
```

---

### **Flow 2: Protected Resource Request**
```
GET /admin
Header: Authorization: Bearer eyJhbGc...

┌─────────────────────────────────────────────────────────────┐
│ 1. Request enters SecurityFilterChain                        │
├─────────────────────────────────────────────────────────────┤
│   authorizeRequests:                                         │
│     /admin → authenticated() (needs auth)                    │
│   sessionManagement:                                         │
│     STATELESS                                                │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 2. AuthTokenFilter.doFilterInternal()                        │
├─────────────────────────────────────────────────────────────┤
│   parseJwt(request) → "eyJhbGc..." ✅                        │
│   jwtUtils.validateJwtToken(jwt) → true ✅                   │
│   username = jwtUtils.getUserNameFromJwtToken(jwt)           │
│            → "user1"                                         │
│                                                              │
│   userDetailsService.loadUserByUsername("user1")             │
│     ↓                                                        │
│   JdbcUserDetailsManager queries database                    │
│     ↓                                                        │
│   Returns UserDetails                                        │
│                                                              │
│   Create authenticated token                                 │
│   SecurityContextHolder.setAuthentication(authentication)    │
│   filterChain.doFilter() → Continue                          │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 3. FilterSecurityInterceptor                                 │
├─────────────────────────────────────────────────────────────┤
│   Check SecurityContext: Is user authenticated? ✅           │
│   Check @PreAuthorize("hasRole('ADMIN')"):                   │
│     User has ROLE_USER ❌                                    │
│     → 403 Forbidden                                          │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ 4. AuthEntryPointJwt (if unauthorized)                       │
├─────────────────────────────────────────────────────────────┤
│   Return: {                                                  │
│     "status": 403,                                           │
│     "error": "Forbidden",                                    │
│     "message": "Access Denied"                               │
│   }                                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Complete Component Interaction Map
```
┌──────────────────────────────────────────────────────────────┐
│                    SecurityConfig                             │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 1. SecurityFilterChain                                    │ │
│ │    ├─ Authorization rules                                 │ │
│ │    ├─ Session: STATELESS                                  │ │
│ │    ├─ Exception handling                                  │ │
│ │    ├─ CSRF: disabled                                      │ │
│ │    └─ Add AuthTokenFilter                                 │ │
│ │                                                            │ │
│ │ 2. AuthTokenFilter Bean ──────────────┐                   │ │
│ │                                        │                   │ │
│ │ 3. UserDetailsService Bean            │                   │ │
│ │    (JdbcUserDetailsManager)           │                   │ │
│ │    └─ Connects to DataSource ─────────┼─────────┐         │ │
│ │                                        │         │         │ │
│ │ 4. PasswordEncoder Bean               │         │         │ │
│ │    (BCryptPasswordEncoder) ───────────┼────┐    │         │ │
│ │                                        │    │    │         │ │
│ │ 5. AuthenticationManager Bean         │    │    │         │ │
│ │    └─ Uses DaoAuthenticationProvider  │    │    │         │ │
│ │        ├─ Uses UserDetailsService ────┘    │    │         │ │
│ │        └─ Uses PasswordEncoder ────────────┘    │         │ │
│ │                                                  │         │ │
│ │ 6. CommandLineRunner                            │         │ │
│ │    └─ Inserts users into database ──────────────┘         │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘